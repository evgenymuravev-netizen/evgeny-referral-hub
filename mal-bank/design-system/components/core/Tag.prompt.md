**Tag** — selectable/removable chip for filters and categories. Pill outline by default; fills with ink when selected.

```jsx
<Tag selected>All</Tag>
<Tag onClick={() => {}}>Travel</Tag>
<Tag removable onRemove={() => {}}>Groceries</Tag>
```

Props: `selected`, `removable` + `onRemove`, `iconLeft`, `onClick`.
