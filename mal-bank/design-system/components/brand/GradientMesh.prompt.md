**GradientMesh** — Mal's signature iridescent pastel background, recreated in CSS. Use as a hero backdrop, feature-card fill, or app header sheet. Overlay dark ink text/icons on top.

```jsx
<GradientMesh variant="hero" style={{ minHeight: 320, padding: 40 }}>
  <h2>The future of finance is here.</h2>
</GradientMesh>
```

Variants: `hero` (full periwinkle→rose→peach), `cool` (calm periwinkle/lavender — good for product UI), `warm` (rose/peach), `veil` (subtle tint over near-white). `grain` adds the soft film texture (on by default). Pair with `--scrim-bottom` for text legibility.
