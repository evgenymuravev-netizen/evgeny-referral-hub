**Switch** — on/off toggle. Ink-filled track when on.

```jsx
<Switch checked={on} onChange={setOn} label="Round-ups" />
```

Props: `checked`, `onChange(next)`, `disabled`, `size` (sm/md), optional `label`.
