**Tooltip** — ink hover/focus label. Wraps a trigger element.

```jsx
<Tooltip label="Halal-verified" side="top">
  <Icon name="shield-check" />
</Tooltip>
```

Props: `label`, `side` (top/bottom/left/right).
